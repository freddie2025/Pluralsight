import Vue from 'vue';
import App from '@/App';

const assert = require('chai').assert;

describe('App.vue', () => {
  it('should render title in book-list through App componenet', () => {
    assert(true, 'No h1 was found in App.vue');
  });
});
